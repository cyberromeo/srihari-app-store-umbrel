# Umbrel Community App Store — TRMNL / Terminus

This repository packages [Terminus](https://github.com/usetrmnl/terminus) — the flagship TRMNL Build-Your-Own-Server (BYOS) application — as an [Umbrel Community App Store](https://github.com/getumbrel/umbrel-community-app-store) app.

Terminus is a Ruby/Hanami web server that lets you manage [TRMNL](https://trmnl.com) e-ink display devices on your own local network or hosted cloud, giving you a private alternative to the TRMNL Core server with full ownership of your data.

## Add this app store to Umbrel

In the umbrelOS UI:

1. Open **Settings → Community App Stores → Add**.
2. Enter this repository's GitHub URL.
3. Once the store appears (listed as **TRMNL App Store**), browse it and install **Terminus**.

## What gets installed

The `trmnl-terminus` app runs the following services via Docker Compose:

| Service   | Image                              | Purpose                                          |
|-----------|------------------------------------|--------------------------------------------------|
| `web`     | `ghcr.io/usetrmnl/terminus:latest` | Hanami/Puma web server on port `2300` (also runs DB migrations on first boot via `APP_SETUP: true`). |
| `worker`  | `ghcr.io/usetrmnl/terminus:latest` | Sidekiq background jobs (firmware sync, screen rendering, etc.). |
| `database`| `postgres:18.4-alpine`             | PostgreSQL 18.4 database.                         |
| `keyvalue`| `valkey/valkey:9-alpine`           | Valkey 9 (Redis-compatible) cache + job queue.    |

Umbrel's `app_proxy` fronts the `web` service at port `2300` and exposes it through the umbrella `*.local` URL.

## First-run setup

Open the Terminus web UI from the umbrelOS app tile and click **Register**. The **first registered user becomes the verified admin**; subsequent registrations require manual verification by the admin. There are no fixed default credentials.

## Configuration

Sensitive values (`APP_SECRET`, `DATABASE_PASSWORD`, `KEYVALUE_PASSWORD`, `API_URI`) are configurable via a `.env` file placed next to `docker-compose.yml` (or auto-generated by the Umbrel manager where applicable). See [`trmnl-terminus/.env.example`](trmnl-terminus/.env.example) for the full list with documented defaults.

⚠️ The inline defaults in `docker-compose.yml` are intentionally insecure placeholders so the app boots standalone. **For any real deployment, override `APP_SECRET`, `DATABASE_PASSWORD`, and `KEYVALUE_PASSWORD` via a `.env` file.** Changing `DATABASE_PASSWORD` or `KEYVALUE_PASSWORD` after first boot requires re-seeding the corresponding volume — see upstream [Docker docs](https://github.com/usetrmnl/terminus/blob/main/doc/docker.adoc#troubleshooting).

## Source

- Upstream project: https://github.com/usetrmnl/terminus
- Upstream issues / support: https://github.com/usetrmnl/terminus/issues
- License: MIT (see upstream repo).

This packaging repo is unaffiliated with TRMNL/the upstream maintainers; it only wraps their published container images for Umbrel.